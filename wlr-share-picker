#!/usr/bin/env python3
"""Launcher to run the picker from the repo without installing: ~/.local/bin/wlr-share-picker → this file."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from wlr_share_picker.cli import main  # noqa: E402

sys.exit(main())
